import React from "react";
import ReactDOM from "react-dom";

const App = () => {
    return (
        <div class="bg-red-500 p-10 text-bold">
            Hello, world!
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById("root"));
